{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "research-plan.schema.json",
  "title": "Research plan",
  "type": "object",
  "additionalProperties": false,
  "required": ["plan_id", "query", "interpreted_scope", "assumptions", "initial_depth", "depth_rationale", "subquestions", "high_risk_areas", "required_source_types", "escalation_triggers", "worker_count", "verification_policy"],
  "properties": {
    "plan_id": { "$ref": "#/$defs/planId" },
    "query": { "type": "string", "minLength": 1 },
    "interpreted_scope": { "type": "string", "minLength": 1 },
    "assumptions": { "type": "array", "items": { "type": "string", "minLength": 1 } },
    "initial_depth": { "enum": ["light", "standard", "deep"] },
    "depth_rationale": { "type": "string", "minLength": 1 },
    "subquestions": { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/subquestion" } },
    "high_risk_areas": { "type": "array", "items": { "type": "string", "minLength": 1 } },
    "required_source_types": { "type": "array", "items": { "$ref": "source.schema.json#/$defs/sourceType" } },
    "escalation_triggers": { "type": "array", "items": { "type": "string", "minLength": 1 } },
    "worker_count": { "type": "integer", "minimum": 1, "maximum": 8 },
    "verification_policy": { "enum": ["none", "risk-based", "all-material"] }
  },
  "$defs": {
    "planId": { "type": "string", "pattern": "^plan_[A-Za-z0-9][A-Za-z0-9_-]*$" },
    "subquestion": {
      "type": "object",
      "additionalProperties": false,
      "required": ["subquestion_id", "question"],
      "properties": {
        "subquestion_id": { "type": "string", "pattern": "^sq_[A-Za-z0-9][A-Za-z0-9_-]*$" },
        "question": { "type": "string", "minLength": 1 }
      }
    }
  }
}
